import React from 'react'
import {useSelector} from "react-redux";
import {selectTypeUnderDeletion} from "./features/menu/menuSlice";

export const Test = () => {
    // const dt = useSelector(selectTypeUnderDeletion) /
    //     console.log(dt)
    return (
        <div></div>
    )
}